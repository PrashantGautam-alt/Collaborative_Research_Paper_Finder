# This file makes the `agents` folder a Python package.
# An empty __init__.py is all Python needs to treat a folder as importable.
# You will import agents like: from agents.search_agent import SearchAgent
